baseDomain: bitspace.org.in

docsRepo:
  repository: bitspaceorg/website/docs/our-awesome-doc
  projectId: 79400481 
  productionBranch: master

vercel:
  # teamId: team_xxxx
  # linkDocsRepo: true

dns:
  provider: cloudflare
  domain: bitspace.org.in
  vercelCnameTarget: cname.vercel-dns.com
  # zoneId: optional; if unset, resolved from domain via API