stages:
  - sync
  - dns
  - deploy
  - cron

default:
  image: node:20-bookworm

validate:projects:
  stage: sync
  before_script:
    - npm ci
  script:
    - npm run validate
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - config.yaml
        - "projects/**"
      when: on_success
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes:
        - config.yaml
        - "projects/**"
      when: on_success
    - when: never

sync-vercel:
  stage: sync
  before_script:
    - npm ci
  script:
    - npm run validate
    - node scripts/sync-vercel.js
  rules:
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_COMMIT_BRANCH == "main"
      changes:
        - config.yaml
        - "projects/**"
      when: on_success
    - when: manual

deploy-docs:
  stage: deploy
  cache:
    key: cron-timestamps
    paths:
      - .cache/
  before_script:
    - npm ci
  script:
    - npm run validate
    - |
      if [ -z "$DOCS_SHA" ]; then
        eval "$(node scripts/get-latest-docs-ref-sha.js)"
      fi
    - node scripts/deploy-docs.js
  rules:
    - if: $CI_PIPELINE_SOURCE == "trigger" && $DOCS_SHA != null && $DOCS_SHA != ""
      when: always
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_COMMIT_BRANCH == "main"
      changes:
        - config.yaml
        - "projects/**"
      when: on_success
    - when: manual

dns:cloudflare:
  stage: dns
  script:
    - npm ci
    - npm run dns
  rules:
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: never
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_COMMIT_BRANCH == "main"
      changes:
        - config.yaml
        - "projects/**"
      when: on_success
    - when: manual

# Run every minute: CI/CD → Schedules → New schedule → Cron: * * * * *
cron:check-data:
  stage: cron
  cache:
    key: cron-timestamps
    paths:
      - .cache/
  before_script:
    - npm ci
  script:
    - node scripts/cron-check-data.js
  artifacts:
    paths:
      - .cache/
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: on_success
    - when: never

# GitLab Pages: last-rebuilt status.
pages:
  stage: .post
  cache:
    key: cron-timestamps
    paths:
      - .cache/
    policy: pull
  before_script:
    - npm ci
  script:
    - node scripts/generate-cron-status-html.js
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      when: on_success
    - if: $CI_PIPELINE_SOURCE == "trigger"
      when: on_success
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: on_success
    - when: never
